# Arduino to PC through RaspberryPi

Examples use an arduino, a raspberry pi and another (whatever) pc.
Type of connection:

Arduino ----serial---- RaspberryPi ----LAN-----PC

Uses sockets. IPs are hard coded.
